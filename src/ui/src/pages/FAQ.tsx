import React, { useState } from 'react';
import Faq from '../components/Faq'; // Import the TrainingUI component

const FAQ: React.FC = () => {
    return (
    
      <div>

        <Faq /> {/* Include the TrainingUI component */}
      </div>
  
    );
  };

export default FAQ;
